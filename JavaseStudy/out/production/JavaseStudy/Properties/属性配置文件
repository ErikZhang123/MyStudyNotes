



配置文件的主要目的是为了能够方便地修改经常变化的数据

将需要修改的数据写入配置文件中，就可以不通过修改java代码就读取到
在开发中就不用重新编译，也不用重启服务器

但注意，配置文件的key，value都为String类型
并且，在属性文件中‘=’等号前后不要出现空格，因为在Spring框架中会出现错误
‘=’可以用‘：’进行代替，但是不建议

属性配置文件在java规范中需要以‘.properties’结尾。
Properties类是专门存放属性配置的类